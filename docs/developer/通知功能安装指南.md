# 通知功能安装指南

## 📌 快速说明

从 v18.4+ 版本开始,MODSDK 工作流的 Hooks 支持桌面通知功能。

## ✅ VSCode 用户

**无需任何配置!** 通知功能开箱即用,会直接显示在 VSCode 右下角。

## 🔧 PyCharm / IntelliJ / WebStorm 用户

需要安装 `plyer` 库以启用系统原生通知:

### Windows

打开命令提示符(CMD)或 PowerShell,执行:

```bash
pip install plyer
```

### macOS / Linux

打开终端,执行:

```bash
pip install plyer

# Linux 用户还需要安装 libnotify (如果尚未安装)
# Ubuntu/Debian:
sudo apt-get install libnotify-bin

# Fedora/CentOS:
sudo yum install libnotify

# Arch Linux:
sudo pacman -S libnotify
```

## 🧪 测试通知

安装完成后,运行测试脚本验证:

```bash
python templates/.claude/hooks/vscode_notify.py
```

**预期结果**:
- 屏幕右下角弹出 3 个系统通知
- 终端输出环境检测信息

## 🔍 故障排查

### Windows: 没有看到通知弹窗

1. 打开"设置" → "系统" → "通知和操作"
2. 找到 "Python" 或 "Windows Terminal"
3. 确保开启了"显示通知横幅"

### macOS: 没有看到通知

1. 打开"系统偏好设置" → "通知"
2. 找到"终端"或"Python"
3. 确保允许通知

### Linux: 没有看到通知

确保 `notify-send` 已安装并可用:

```bash
# 测试 notify-send
notify-send "测试" "Hello World"

# 如果失败,安装 libnotify
sudo apt-get install libnotify-bin  # Ubuntu/Debian
```

### plyer 安装失败

如果 pip 安装失败,尝试:

```bash
# 方案1: 使用 --user 标志
pip install --user plyer

# 方案2: 升级 pip
python -m pip install --upgrade pip
pip install plyer

# 方案3: 使用国内镜像(如果网络问题)
pip install -i https://pypi.tuna.tsinghua.edu.cn/simple plyer
```

## 📋 通知触发场景

| 场景 | 通知级别 | 通知内容 |
|------|----------|----------|
| 执行 `/mc` 命令 | 信息 | "任务追踪已初始化" |
| 任务失败 1 次 | 警告 | "任务未完成(失败 1 次)" |
| 任务失败 ≥2 次 | 错误 | "任务失败 N 次,需要专家审核" |
| 任务完成 | 信息 | "任务已完成并归档" |
| 专家审核评分 < 8 | 错误 | "专家审核未通过" |
| 专家审核评分 ≥ 8 | 信息 | "专家审核通过" |

## 📖 不想安装 plyer?

**完全可以!** 即使不安装 plyer,通知系统仍会工作:

- 自动降级为**彩色终端输出**
- 使用 Emoji 图标增强可读性
- 所有重要信息都会显示在终端

示例输出:
```
ℹ️ 任务追踪已初始化 - 任务ID: task-20250113-143022
⚠️ 任务未完成(失败 1 次) - 会话已被阻止
✅ 任务已完成并归档 - 已记录到 tasks/task-xxx/
```

## 📚 更多信息

- [通知系统技术文档](./developer/通知系统.md)
- [Hook 机制说明](../templates/.claude/hooks/README.md)

---

**版本**: v18.4.0
**最后更新**: 2025-01-13
