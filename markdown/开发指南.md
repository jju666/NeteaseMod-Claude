# 开发指南

> **📍 导航**: [🏠 首页](../README.md) > [📂 文档](../README.md#文档导航) > 开发指南
>
> 📖 完整的开发流程、代码示例和最佳实践
>
> **📅 最后更新**: 2025-11-09

---

## 📋 目录

1. [开发工作流](#1-开发工作流)
2. [如何参考官方文档](#2-如何参考官方文档)
3. [添加新的游戏系统](#3-添加新的游戏系统)
4. [添加新的预设类型](#4-添加新的预设类型)
5. [添加新的状态](#5-添加新的状态)
6. [配置管理](#6-配置管理)
7. [调试与测试](#7-调试与测试)
8. [完整工作流示例](#8-完整工作流示例)

---

## 1. 开发工作流

### 1.1 标准开发流程

使用Claude Code开发时的标准流程：

```
1. 阅读相关文档（架构、规范）
   ↓
2. 查看项目状态.md了解功能清单
   ↓
3. 参考官方文档（如需要）
   ↓
4. 编写代码（遵循开发规范）
   ↓
5. 运行测试验证
   ↓
6. 更新文档
```

详见 [项目状态.md](./项目状态.md)

### 1.2 使用Claude Code的三步流程

**步骤1: 理解任务与分级（2分钟）**

- 判断任务级别：微任务/标准任务/复杂任务
- 检查历史上下文：`dir tasks /b`
- 微任务直接执行：跳过步骤2-3

**步骤2: 查阅文档（仅标准/复杂任务）**

- 优先级1: 开发规范.md（CRITICAL规范）
- 优先级2: 问题排查.md（已知问题）
- 优先级3: Systems文档（系统实现）
- 优先级4: 官方文档（不熟悉API时）

**步骤3: 执行与收尾**

- 标准任务：创建5章模板 → 实施 → Git commit
- 复杂任务：创建9章模板 → 分阶段实施 → Git commit

详见 [CLAUDE.md](../CLAUDE.md)

### 1.3 任务分级快速参考

| 级别 | 特征 | 示例 | Token成本 |
|-----|------|------|-----------|
| 🟢 微任务 | 单文件<30行 | "调整配置参数" | <2k |
| 🟡 标准任务 | 3-8文件 | "修复预设交互" | 20-35k |
| 🔴 复杂任务 | >8文件/架构 | "添加新系统" | 40-60k |

[⬆️ 返回目录](#目录)

---

## 2. 如何参考官方文档

### 2.1 何时需要查阅官方文档

✅ **应该查阅的情况**:
- 遇到不熟悉的API时
- 需要了解组件/系统用法时
- 查询事件参数和返回值时
- 涉及原版实体、物品、NBT时
- 需要了解原版游戏机制时

❌ **不需要查阅的情况**:
- 本地文档已有详细说明
- CRITICAL规范已明确禁止
- 问题排查.md已有解决方案

### 2.2 网易MODSDK官方文档

**GitHub仓库**: https://github.com/EaseCation/netease-modsdk-wiki

**查阅方式**:

使用WebFetch工具在线获取最新文档：

```python
WebFetch(
    url="https://raw.githubusercontent.com/EaseCation/netease-modsdk-wiki/main/docs/mcdocs/1-ModAPI/...",
    prompt="提取关于NotifyToClient的参数说明和使用示例"
)
```

**常用查阅路径**:

| 需求 | 路径 | 示例URL |
|------|------|---------|
| **API接口** | `/docs/mcdocs/1-ModAPI/接口/` | `/接口/玩家/背包.md` |
| **事件系统** | `/docs/mcdocs/1-ModAPI/事件/` | `/事件/服务端事件.md` |
| **组件系统** | `/docs/mcdocs/1-ModAPI/组件/` | `/组件/实体组件.md` |
| **枚举类型** | `/docs/mcdocs/1-ModAPI/枚举/` | `/枚举/ItemPosType.md` |

**查阅示例**:

```python
# 示例1: 查询SetPlayerAllItems API用法
WebFetch(
    url="https://raw.githubusercontent.com/EaseCation/netease-modsdk-wiki/main/docs/mcdocs/1-ModAPI/接口/玩家/背包.md",
    prompt="提取SetPlayerAllItems方法的参数说明、返回值和使用示例"
)

# 示例2: 查询ServerPlayerJoinEvent事件
WebFetch(
    url="https://raw.githubusercontent.com/EaseCation/netease-modsdk-wiki/main/docs/mcdocs/1-ModAPI/事件/服务端事件.md",
    prompt="提取ServerPlayerJoinEvent的事件数据字段和触发时机"
)
```

### 2.3 基岩版Wiki

**GitHub仓库**: https://github.com/Bedrock-OSS/bedrock-wiki

**何时查阅**:
- 涉及原版实体、物品、方块时
- NBT数据结构相关问题时
- 需要了解原版游戏机制时

**常用页面**:

| 需求 | 路径 | 说明 |
|------|------|------|
| **NBT结构** | `/docs/nbt/` | 物品NBT、实体NBT |
| **实体组件** | `/docs/entities/` | 实体行为、AI组件 |
| **物品格式** | `/docs/items/` | 物品定义、属性 |
| **方块格式** | `/docs/blocks/` | 方块状态、属性 |

**查阅示例**:

```python
# 示例: 查询物品NBT结构
WebFetch(
    url="https://raw.githubusercontent.com/Bedrock-OSS/bedrock-wiki/main/docs/nbt/items.md",
    prompt="提取物品NBT的必需字段和可选字段定义"
)
```

### 2.4 查阅结果记录

查阅完官方文档后，应在任务上下文中记录：

```markdown
### 核心检查点

✅ 已查阅文档:
- 开发规范.md - System生命周期
- 网易MODSDK Wiki - NotifyToClient API

🎯 提取原则:
- ⛔ 禁止在__init__中调用GetComponent
- ✅ 应该在Create()中初始化组件
- 📚 原因: __init__时游戏未初始化

📄 文档依据:
- 开发规范.md:152-295
- API文档: SetPlayerAllItems返回值为bool

🌐 官方文档查阅:
- 查询NotifyToClient参数说明（成功）
- 查询物品NBT结构（成功）
```

[⬆️ 返回目录](#目录)

---

## 3. 添加新的游戏系统

### 3.1 System开发概述

网易MODSDK中，System是核心逻辑单元：

- **ServerSystem**: 服务端逻辑，游戏规则、数据处理
- **ClientSystem**: 客户端逻辑，UI显示、视觉效果

### 3.2 创建ServerSystem

**步骤1: 创建系统文件**

位置：`Script_NeteaseMod/systems/MyNewSystem.py`

```python
# -*- coding: utf-8 -*-
"""新系统说明
功能: XXX
负责: YYY
"""

from __future__ import print_function
import mod.server.extraServerApi as serverApi
from Script_NeteaseMod.modConfig import MOD_NAME

class MyNewSystem(serverApi.GetServerSystemCls()):
    def __init__(self, namespace, systemName):
        super(MyNewSystem, self).__init__(namespace, systemName)

        # 初始化成员变量
        self.players = {}
        self.config = None

        # ⚠️ CRITICAL: 手动调用Create()
        # 网易引擎不会自动调用Create()，必须手动调用
        print("[MyNewSystem] 手动调用Create()完成系统初始化")
        self.Create()

    def Create(self):
        """系统创建时的初始化逻辑"""
        print("[MyNewSystem] Create() 被调用")

        # 注册事件监听
        self.ListenForEvent(
            "Minecraft",
            serverApi.GetEngineNamespace(),
            "ServerPlayerJoinEvent",
            self.OnPlayerJoin
        )

        # 加载配置
        self.config = self._load_config()

    def Destroy(self):
        """系统销毁时自动被引擎调用"""
        print("[MyNewSystem] Destroy() 被调用")

        # 清理资源
        self.players.clear()
        self.config = None

    def OnPlayerJoin(self, args):
        """玩家加入事件处理"""
        player_id = args.get("id")
        print("[MyNewSystem] 玩家加入:", player_id)
        self.players[player_id] = {}

    def _load_config(self):
        """加载配置（私有方法）"""
        return {}
```

**步骤2: 注册System**

位置：`Script_NeteaseMod/modConfig.py`

```python
# 服务端System列表
SERVER_SYSTEMS = [
    # ... 现有系统
    ("MyNewSystem", "Script_NeteaseMod.systems.MyNewSystem.MyNewSystem"),
]
```

**步骤3: 验证注册**

运行游戏，检查日志：

```
[INFO] [MyNewSystem] 手动调用Create()完成系统初始化
[INFO] [MyNewSystem] Create() 被调用
```

### 3.3 创建ClientSystem

**步骤1: 创建系统文件**

位置：`Script_NeteaseMod/systems/MyNewClientSystem.py`

```python
# -*- coding: utf-8 -*-
"""新客户端系统说明
功能: XXX
负责: UI显示、视觉效果
"""

from __future__ import print_function
import mod.client.extraClientApi as clientApi
from Script_NeteaseMod.modConfig import MOD_NAME

class MyNewClientSystem(clientApi.GetClientSystemCls()):
    def __init__(self, namespace, systemName):
        super(MyNewClientSystem, self).__init__(namespace, systemName)

        # 初始化成员变量
        self.ui_comp = None

        # ⚠️ CRITICAL: 手动调用Create()
        print("[MyNewClientSystem] 手动调用Create()完成系统初始化")
        self.Create()

    def Create(self):
        """系统创建时的初始化逻辑"""
        print("[MyNewClientSystem] Create() 被调用")

        # 获取UI组件
        self.ui_comp = self.GetComponent("Minecraft", "ui")

        # 注册客户端事件
        self.ListenForEvent(
            MOD_NAME,
            serverApi.GetEngineNamespace(),
            "MyCustomEvent",
            self.OnCustomEvent
        )

    def Destroy(self):
        """系统销毁时自动被引擎调用"""
        print("[MyNewClientSystem] Destroy() 被调用")
        self.ui_comp = None

    def OnCustomEvent(self, args):
        """自定义事件处理"""
        data = args.get("data")
        print("[MyNewClientSystem] 收到事件:", data)
```

**步骤2: 注册ClientSystem**

位置：`Script_NeteaseMod/modConfig.py`

```python
# 客户端System列表
CLIENT_SYSTEMS = [
    # ... 现有系统
    ("MyNewClientSystem", "Script_NeteaseMod.systems.MyNewClientSystem.MyNewClientSystem"),
]
```

### 3.4 双端通信实现

**服务端发送事件到客户端**:

```python
# 发送给单个玩家
self.NotifyToClient(player_id, "MyCustomEvent", {"data": "Hello"})

# 广播给所有玩家
self.BroadcastToAllClient("MyCustomEvent", {"data": "Hello"})
```

**客户端发送事件到服务端**:

```python
# 发送给服务端
self.NotifyToServer("MyServerEvent", {"data": "Hello from client"})
```

**⚠️ CRITICAL规范**:

- ❌ 禁止在服务端GetSystem获取客户端系统
- ❌ 禁止在客户端GetSystem获取服务端系统
- ✅ 使用NotifyToClient/NotifyToServer通信

详见 [开发规范.md - 双端隔离原则](./开发规范.md#双端隔离原则)

[⬆️ 返回目录](#目录)

---

## 4. 添加新的预设类型

### 4.1 ECPreset框架概述

ECPreset采用**单例PresetDefinition + 多例PresetInstance**架构：

- **PresetDefinition（预设定义）**: 单例，所有同类型预设共享
- **PresetInstance（预设实例）**: 多例，每个预设有独立实例

### 4.2 创建服务端Preset

**步骤1: 创建PresetDefinition文件**

位置：`Script_NeteaseMod/presets/server/MyPresetDefServer.py`

```python
# -*- coding: utf-8 -*-
"""新预设服务端定义
类型: my_preset_type
功能: XXX
"""

from __future__ import print_function
import mod.server.extraServerApi as serverApi
from ECPresetMod.server.preset import PresetDefinitionServer
from Script_NeteaseMod.modConfig import MOD_NAME

class MyPresetDefServer(PresetDefinitionServer):
    # 启用tick更新（可选）
    enable_tick = True

    def on_init(self, instance):
        """预设实例创建时调用"""
        print("[MyPreset] on_init:", instance.get_id())

        # ✅ 正确：将实例级数据存储在instance.data中
        instance.set_data("counter", 0)
        instance.set_data("created_time", import time; time.time())

    def on_start(self, instance):
        """预设启动时调用"""
        print("[MyPreset] on_start:", instance.get_id())

        # 从配置读取位置
        position = instance.get_config("position")
        print("[MyPreset] 位置:", position)

        # 创建实体
        entity_id = self._create_entity(instance, position)
        instance.set_data("entity_id", entity_id)

    def on_tick(self, instance, dt):
        """每tick调用（需enable_tick=True）"""
        # ⚠️ 必须接受dt参数！
        counter = instance.get_data("counter", 0)
        instance.set_data("counter", counter + 1)

        if counter % 20 == 0:  # 每秒执行一次
            print("[MyPreset] tick:", counter)

    def on_stop(self, instance):
        """预设停止时调用"""
        print("[MyPreset] on_stop:", instance.get_id())

        # 清理实体
        entity_id = instance.get_data("entity_id")
        if entity_id:
            self._destroy_entity(entity_id)

    def on_destroy(self, instance):
        """预设销毁时调用"""
        print("[MyPreset] on_destroy:", instance.get_id())

    def _create_entity(self, instance, position):
        """创建实体（私有方法）"""
        # 实现实体创建逻辑
        return "entity_id_123"

    def _destroy_entity(self, entity_id):
        """销毁实体（私有方法）"""
        # 实现实体销毁逻辑
        pass
```

**步骤2: 注册Preset类型**

位置：`Script_NeteaseMod/modConfig.py`

```python
# 服务端Preset类型定义
SERVER_PRESET_TYPES = [
    # ... 现有预设
    ("my_preset_type", "Script_NeteaseMod.presets.server.MyPresetDefServer.MyPresetDefServer"),
]
```

### 4.3 创建客户端Preset（可选）

位置：`Script_NeteaseMod/presets/client/MyPresetDefClient.py`

```python
# -*- coding: utf-8 -*-
"""新预设客户端定义
类型: my_preset_type
功能: UI显示、视觉效果
"""

from __future__ import print_function
import mod.client.extraClientApi as clientApi
from ECPresetMod.client.preset import PresetDefinitionClient

class MyPresetDefClient(PresetDefinitionClient):
    enable_tick = True

    def on_init(self, instance):
        """预设实例创建时调用"""
        print("[MyPresetClient] on_init:", instance.get_id())
        instance.set_data("ui_visible", False)

    def on_start(self, instance):
        """预设启动时调用"""
        print("[MyPresetClient] on_start:", instance.get_id())
        self._create_ui(instance)

    def on_tick(self, instance, dt):
        """每tick调用"""
        # 更新UI显示
        pass

    def on_stop(self, instance):
        """预设停止时调用"""
        self._destroy_ui(instance)

    def on_destroy(self, instance):
        """预设销毁时调用"""
        pass

    def _create_ui(self, instance):
        """创建UI（私有方法）"""
        pass

    def _destroy_ui(self, instance):
        """销毁UI（私有方法）"""
        pass
```

注册到`modConfig.py`:

```python
# 客户端Preset类型定义
CLIENT_PRESET_TYPES = [
    # ... 现有预设
    ("my_preset_type", "Script_NeteaseMod.presets.client.MyPresetDefClient.MyPresetDefClient"),
]
```

### 4.4 数据存储规则 ⚠️ CRITICAL

```python
# ❌ 错误：将实例级数据存储在self中
class MyPresetDefServer(PresetDefinitionServer):
    def on_start(self, instance):
        self.entity_id = create_entity()  # ❌ 错误！会被后续实例覆盖

# ✅ 正确：将实例级数据存储在instance.data中
class MyPresetDefServer(PresetDefinitionServer):
    def on_start(self, instance):
        entity_id = create_entity()
        instance.set_data("entity_id", entity_id)  # ✅ 正确！

    def on_stop(self, instance):
        entity_id = instance.get_data("entity_id")  # ✅ 读取
```

**数据存储原则**:
- **实例级数据**（如entity_id、倒计时时间）→ `instance.set_data()` / `instance.get_data()`
- **全局配置**（如资源类型配置）→ `self`成员变量
- **预设配置**（如位置、队伍）→ `instance.get_config()`

详见 [开发规范.md - ECPreset预设开发](./开发规范.md#ecpreset预设开发-critical)

[⬆️ 返回目录](#目录)

---

## 5. 添加新的状态

### 5.1 状态机架构概述

状态机用于管理游戏流程：

- **GamingState**: 基础状态类
- **TimedGamingState**: 带超时的状态类
- **RootGamingState**: 根状态类，管理子状态切换

### 5.2 创建基础状态

**步骤1: 创建状态文件**

位置：`Script_NeteaseMod/systems/states/MyNewState.py`

```python
# -*- coding: utf-8 -*-
"""新状态说明
功能: XXX
流转: PreviousState → MyNewState → NextState
"""

from __future__ import print_function
from Script_NeteaseMod.systems.state.GamingState import GamingState

class MyNewState(GamingState):
    def __init__(self, system_context):
        super(MyNewState, self).__init__(system_context)

    def on_enter(self):
        """进入状态时调用"""
        print("[MyNewState] on_enter")

        # 注册事件监听（状态退出时自动取消）
        self.listen_event(
            "ServerPlayerJoinEvent",
            self.on_player_join
        )

        # 执行状态逻辑
        self._do_something()

        # 切换到下一个状态（可选）
        # self.switch_to_sub_state("NextState")

    def on_exit(self):
        """退出状态时调用"""
        print("[MyNewState] on_exit")
        # 事件监听会自动取消，无需手动UnListenForEvent

    def on_player_join(self, args):
        """玩家加入事件处理"""
        player_id = args.get("id")
        print("[MyNewState] 玩家加入:", player_id)

    def _do_something(self):
        """状态内部逻辑（私有方法）"""
        pass
```

**步骤2: 注册到状态树**

在父状态中注册子状态：

```python
class ParentState(GamingState):
    def __init__(self, system_context):
        super(ParentState, self).__init__(system_context)

        # 注册子状态
        self.register_sub_state("MyNewState", MyNewState)
        self.register_sub_state("NextState", NextState)

    def on_enter(self):
        """进入父状态时，切换到第一个子状态"""
        self.switch_to_sub_state("MyNewState")
```

### 5.3 创建定时状态

```python
from Script_NeteaseMod.systems.state.TimedGamingState import TimedGamingState

class MyTimedState(TimedGamingState):
    def __init__(self, system_context):
        super(MyTimedState, self).__init__(
            system_context,
            timeout_seconds=10  # 10秒后自动超时
        )

    def on_enter(self):
        """进入状态时调用"""
        print("[MyTimedState] on_enter, 倒计时10秒")

    def on_timeout(self):
        """超时后调用"""
        print("[MyTimedState] on_timeout, 切换到下一个状态")
        self.switch_to_state("NextState")
```

### 5.4 状态切换方式

```python
# ✅ 正确：切换到同级状态
self.switch_to_state("NextState")

# ✅ 正确：切换到子状态
self.switch_to_sub_state("ChildState")

# ❌ 错误：不要手动管理状态
self.current_state = NextState(self.system)  # ❌ 错误
```

### 5.5 事件监听管理

```python
# ✅ 正确：使用listen_event()会在状态退出时自动取消
def on_enter(self):
    self.listen_event("ServerPlayerJoinEvent", self.on_player_join)
    # 状态退出时自动UnListenForEvent

# ❌ 错误：直接ListenForEvent不会自动清理
def on_enter(self):
    self.system.ListenForEvent(
        "ServerPlayerJoinEvent",
        "Minecraft",
        self.on_player_join
    )  # 状态退出后事件仍然监听，可能导致错误
```

[⬆️ 返回目录](#目录)

---

## 6. 配置管理

### 6.1 modConfig.py结构

位置：`Script_NeteaseMod/modConfig.py`

```python
# -*- coding: utf-8 -*-
"""模组配置文件"""

# ===== 基础信息 =====
MOD_NAME = "Script_NeteaseMod"
MOD_VERSION = "1.0.0"

# ===== 服务端System注册 =====
SERVER_SYSTEMS = [
    ("MySystem", "Script_NeteaseMod.systems.MySystem.MySystem"),
]

# ===== 客户端System注册 =====
CLIENT_SYSTEMS = [
    ("MyClientSystem", "Script_NeteaseMod.systems.MyClientSystem.MyClientSystem"),
]

# ===== 服务端Preset类型注册 =====
SERVER_PRESET_TYPES = [
    ("my_preset", "Script_NeteaseMod.presets.server.MyPresetDefServer.MyPresetDefServer"),
]

# ===== 客户端Preset类型注册 =====
CLIENT_PRESET_TYPES = [
    ("my_preset", "Script_NeteaseMod.presets.client.MyPresetDefClient.MyPresetDefClient"),
]
```

### 6.2 配置文件组织

**推荐目录结构**:

```
Script_NeteaseMod/
├── modConfig.py          # 主配置
├── config/               # 配置文件目录
│   ├── game_config.py    # 游戏配置
│   ├── shop_config.py    # 商店配置
│   └── item_config.py    # 物品配置
```

**示例：商店配置**

位置：`Script_NeteaseMod/config/shop_config.py`

```python
# -*- coding: utf-8 -*-
"""商店配置"""

from __future__ import print_function

# 商店商品配置
SHOP_ITEMS = {
    "diamond_sword": {
        "name": u"钻石剑",
        "item": "minecraft:diamond_sword",
        "price": 15,
        "currency": "diamond"
    },
    "iron_block": {
        "name": u"铁块",
        "item": "minecraft:iron_block",
        "price": 10,
        "currency": "iron"
    }
}

# 商店窗口配置
SHOP_CATEGORIES = [
    {
        "id": "weapons",
        "name": u"武器",
        "items": ["diamond_sword", "iron_sword"]
    },
    {
        "id": "blocks",
        "name": u"方块",
        "items": ["iron_block", "wool"]
    }
]
```

### 6.3 配置导入规范 ⚠️ CRITICAL

```python
# ✅ 正确：在modMain.py同级文件中使用相对导入
from modConfig import MOD_NAME, MOD_VERSION

# ✅ 正确：在子目录文件中使用完整路径
from Script_NeteaseMod.modConfig import MOD_NAME
from Script_NeteaseMod.config.shop_config import SHOP_ITEMS

# ❌ 错误：在子目录中使用相对导入
from modConfig import MOD_NAME  # ImportError!
```

详见 [开发规范.md - 模块导入规范](./开发规范.md#模块导入规范-critical)

[⬆️ 返回目录](#目录)

---

## 7. 调试与测试

### 7.1 日志输出技巧

**推荐的日志格式**:

```python
# ✅ 推荐格式
print("[INFO] [系统名] 消息内容")
print("[ERROR] [系统名] 错误信息")
print("[DEBUG] [系统名] 调试信息")

# 示例
print("[INFO] [BedWarsGameSystem] 玩家加入:", player_id)
print("[ERROR] [ShopSystem] 商品不存在:", item_id)
print("[DEBUG] [TeamModule] 队伍数据:", team_data)
```

**日志级别控制**:

```python
DEBUG = True  # 开发环境设为True，生产环境设为False

def log_debug(msg):
    if DEBUG:
        print("[DEBUG] [MySystem]", msg)

# 使用
log_debug("调试信息")  # 生产环境不输出
```

### 7.2 常用调试命令

**游戏内命令**:

```bash
# 传送玩家
/tp <player> <x> <y> <z>

# 给予物品
/give <player> <item> [amount]

# 切换游戏模式
/gamemode <mode> <player>

# 播放音效
/playsound <sound> <player> [pos] [volume] [pitch]

# 显示Title
/title <player> title <text>
/title <player> subtitle <text>
```

**Python调试技巧**:

```python
# 打印变量类型和值
print("[DEBUG] type(obj):", type(obj), "value:", obj)

# 打印字典内容
import json
print("[DEBUG] data:", json.dumps(data, indent=2))

# 异常捕获
try:
    # 你的代码
    do_something()
except Exception as e:
    print("[ERROR] 发生异常:", e)
    import traceback
    traceback.print_exc()
```

### 7.3 性能分析

**Tick性能优化**:

```python
# ✅ 正确：使用计数器降低频率
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        super(MySystem, self).__init__(namespace, systemName)
        self.tick_counter = 0
        self.Create()

    def Update(self):
        """每tick调用"""
        self.tick_counter += 1

        # 每20 tick（1秒）执行一次
        if self.tick_counter % 20 == 0:
            self.expensive_operation()

    def expensive_operation(self):
        """耗时操作"""
        pass

# ❌ 错误：每tick都执行耗时操作
class MySystem(ServerSystem):
    def Update(self):
        self.expensive_operation()  # 性能问题！
```

**时间测量**:

```python
import time

start_time = time.time()
# 执行操作
do_something()
elapsed_time = time.time() - start_time
print("[DEBUG] 耗时:", elapsed_time, "秒")
```

### 7.4 验证方法

**功能验证清单**:

- [ ] 系统是否正确注册（检查日志）
- [ ] 事件监听是否生效（触发事件验证）
- [ ] 数据是否正确存储（查询验证）
- [ ] UI是否正确显示（客户端验证）
- [ ] 性能是否符合要求（压力测试）

[⬆️ 返回目录](#目录)

---

## 8. 完整工作流示例

### 8.1 案例1: 添加商店系统

**任务描述**: 实现一个简单的商店系统，玩家可以购买物品。

**任务分级**: 🟡 标准任务（3-8文件）

**步骤1: 理解任务**

- 需要文件：ShopSystem.py, shop_config.py
- 预计行数：约200行
- 任务级别：标准任务，使用5章模板

**步骤2: 查阅文档**

```markdown
✅ 已查阅文档:
- 开发规范.md - System生命周期、模块导入规范
- 问题排查.md - 商店配置案例

🎯 提取原则:
- ⛔ 禁止在__init__中调用GetComponent
- ✅ 应该在Create()中初始化组件
- ✅ 配置文件使用from...import导入

📄 文档依据:
- 开发规范.md:152-295 (System生命周期)
- 开发规范.md:296-500 (模块导入规范)
```

**步骤3: 执行开发**

**3.1 创建配置文件**

`Script_NeteaseMod/config/shop_config.py`:

```python
# -*- coding: utf-8 -*-
"""商店配置"""

SHOP_ITEMS = {
    "diamond_sword": {
        "name": u"钻石剑",
        "item": "minecraft:diamond_sword",
        "price": 15,
        "currency": "diamond"
    }
}
```

**3.2 创建System文件**

`Script_NeteaseMod/systems/ShopSystem.py`:

```python
# -*- coding: utf-8 -*-
"""商店系统"""

from __future__ import print_function
import mod.server.extraServerApi as serverApi
from Script_NeteaseMod.modConfig import MOD_NAME
from Script_NeteaseMod.config.shop_config import SHOP_ITEMS

class ShopSystem(serverApi.GetServerSystemCls()):
    def __init__(self, namespace, systemName):
        super(ShopSystem, self).__init__(namespace, systemName)
        self.shops = {}
        self.Create()

    def Create(self):
        print("[ShopSystem] Create() 被调用")
        self.ListenForEvent(
            MOD_NAME,
            serverApi.GetEngineNamespace(),
            "BuyItemEvent",
            self.OnBuyItem
        )

    def OnBuyItem(self, args):
        player_id = args.get("player_id")
        item_id = args.get("item_id")

        if item_id not in SHOP_ITEMS:
            print("[ERROR] [ShopSystem] 商品不存在:", item_id)
            return

        item_config = SHOP_ITEMS[item_id]
        # 购买逻辑...
        print("[ShopSystem] 玩家购买:", player_id, item_id)
```

**3.3 注册System**

`Script_NeteaseMod/modConfig.py`:

```python
SERVER_SYSTEMS = [
    # ... 现有系统
    ("ShopSystem", "Script_NeteaseMod.systems.ShopSystem.ShopSystem"),
]
```

**3.4 验证结果**

运行游戏，检查日志：

```
[INFO] [ShopSystem] Create() 被调用
[INFO] [ShopSystem] 玩家购买: -4294967295 diamond_sword
```

**3.5 Git提交**

```bash
git add .
git commit -m "feat: 添加商店系统

- 新增ShopSystem服务端系统
- 新增shop_config.py配置文件
- 实现商品购买逻辑

🤖 Generated with Claude Code(https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

### 8.2 案例2: 修复System生命周期BUG

**任务描述**: 修复MySystem初始化错误，GetComponent返回None。

**任务分级**: 🟢 微任务（单文件<30行）

**步骤1: 快速通道**

直接跳过查阅文档，使用Edit工具修复。

**步骤2: 诊断问题**

查看代码：

```python
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        super(MySystem, self).__init__(namespace, systemName)
        # ❌ 错误：在__init__中调用GetComponent
        self.comp = self.GetComponent("Minecraft", "item")
```

**步骤3: 修复代码**

```python
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        super(MySystem, self).__init__(namespace, systemName)
        self.comp = None
        self.Create()  # ✅ 手动调用Create()

    def Create(self):
        # ✅ 在Create中初始化组件
        self.comp = self.GetComponent("Minecraft", "item")
```

**步骤4: Git提交**

```bash
git add .
git commit -m "fix: 修复MySystem初始化错误

遵循CRITICAL规范，在Create()中初始化组件

🤖 Generated with Claude Code(https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

### 8.3 案例3: 添加床位预设

**任务描述**: 添加床位预设，管理队伍床位。

**任务分级**: 🟡 标准任务（3-8文件）

**省略详细步骤**（参考案例1流程）

关键要点：

- 创建`BedPresetDefServer.py`和`BedPresetDefClient.py`
- 数据存储使用`instance.set_data()`而非`self`
- on_tick方法必须接受dt参数
- 位置信息使用`instance.get_config("position")`

[⬆️ 返回目录](#目录)

---

## 📚 相关文档

### 重要参考

- [开发规范](./开发规范.md) - CRITICAL规范，避免常见错误（必读）
- [问题排查](./问题排查.md) - 已知问题解决方案
- [快速开始](./快速开始.md) - 环境配置和快速入门

### 架构文档

- [CLAUDE.md](../CLAUDE.md) - AI工作流程参考
- [项目状态](./项目状态.md) - 功能清单和开发进度

### AI辅助文档

- [任务类型决策表](./ai/任务类型决策表.md) - 任务分级指南
- [快速通道流程](./ai/快速通道流程.md) - 微任务执行
- [上下文管理规范](./ai/上下文管理规范.md) - Tasks目录管理

### 官方资源

- [网易MODSDK Wiki](https://github.com/EaseCation/netease-modsdk-wiki) - 官方API文档
- [基岩版Wiki](https://github.com/Bedrock-OSS/bedrock-wiki) - 原版机制参考

---

**[⬆️ 返回顶部](#开发指南)** | **[🏠 返回首页](../README.md)** | **[➡️ 项目状态](./项目状态.md)**

---

_最后更新: 2025-11-09 | 文档版本: 1.0_
