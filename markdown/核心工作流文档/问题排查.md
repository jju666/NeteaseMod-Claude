# 问题排查指南

> **📍 导航**: [🏠 首页](../README.md) > [📂 文档](../README.md#文档导航) > 问题排查
>
> 🔧 常见问题解决方案、调试技巧和性能优化建议
>
> **📅 最后更新**: 2025-11-11
> **文档版本**: 4.1 (新增网络/性能/架构问题)

---

## 📋 目录

1. [常见问题（FAQ）](#常见问题faq)
   - [问题1：跨端GetSystem返回None](#问题1跨端getsystem返回none)
   - [问题2：__init__中调用API返回None](#问题2__init__中调用api返回none)
   - [问题3：EventData序列化失败](#问题3eventdata序列化失败)
   - [问题4：模块导入错误](#问题4模块导入错误---typeerror-module-object-is-not-callable)
   - [问题5：子目录导入失败](#问题5子目录导入失败---importerror-no-module-named-modconfig)
   - [问题6：实体无法交互](#问题6实体无法交互)
   - [问题7：客户端事件无法接收](#问题7客户端事件无法接收)
   - [问题8：配置文件读取失败](#问题8配置文件读取失败)
   - [问题9：AOI感应区不生效](#问题9aoi感应区不生效)
   - [问题10：联机大厅Mod不生效](#问题10联机大厅mod不生效)
   - [问题11：Claude Code斜杠命令报错"空文本块"](#问题11claude-code斜杠命令报错空文本块)
   - [问题12：游戏卡顿，服务器TPS降低](#问题12游戏卡顿服务器tps降低) ⭐ 新增
   - [问题13：网络延迟高，事件不同步](#问题13网络延迟高事件不同步) ⭐ 新增
   - [问题14：System未使用RegisterView导致性能下降](#问题14system未使用registerview导致性能下降) ⭐ 新增
   - [问题15：内存持续增长最终崩溃](#问题15内存持续增长最终崩溃) ⭐ 新增
2. [调试技巧](#调试技巧)
3. [网易引擎限制](#网易引擎限制)
4. [性能问题](#性能问题)

---

## 常见问题（FAQ）

### 问题1：跨端GetSystem返回None

**症状**：服务端尝试GetSystem获取客户端系统返回None，调用方法报AttributeError

**错误代码**：
```python
# ❌ 错误示例
class ServerSystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        # 尝试获取客户端System
        clientSys = serverApi.GetSystem("MyMod", "MyClientSystem")  # 返回None!
        clientSys.DoSomething()  # AttributeError: 'NoneType' object has no attribute
```

**原因**：Server和Client运行在**完全独立的进程**中，无法跨端GetSystem

**解决方案**：使用双端通信

```python
# ✅ 正确 - 服务端发送事件到客户端
class ServerSystem(ServerSystem):
    def NotifyClient(self, playerId):
        data = self.CreateEventData()
        data["message"] = "Hello"
        self.NotifyToClient(playerId, "UpdateUI", data)

# ✅ 正确 - 客户端监听服务端事件
class ClientSystem(ClientSystem):
    def Create(self):
        self.ListenForEvent("MyMod", "MyServerSystem", "UpdateUI", self, self.OnUpdate)
```

**详见**：[开发规范.md - 双端隔离原则](./开发规范.md#21-双端隔离原则-)

---

### 问题2：__init__中调用API返回None

**症状**：在System的`__init__`中调用GetComponent、ListenForEvent等API失败或返回None

**错误代码**：
```python
# ❌ 错误示例
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        # 在__init__中调用API
        self.gameComp = serverApi.GetEngineCompFactory().CreateGame(levelId)  # None!
        self.ListenForEvent(...)  # 可能失败
```

**原因**：`__init__`时游戏引擎未完成初始化，实体/组件未注册

**解决方案**：手动调用Create()

```python
# ✅ 正确示例
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.gameComp = None  # 只声明
        self.Create()  # ⚠️ 手动调用

    def Create(self):
        """✅ 在这里初始化"""
        levelId = serverApi.GetLevelId()
        self.gameComp = serverApi.GetEngineCompFactory().CreateGame(levelId)
        self.ListenForEvent(...)
```

**详见**：[开发规范.md - System生命周期限制](./开发规范.md#22-system生命周期限制-)

---

### 问题3：EventData序列化失败

**症状**：使用NotifyToClient/NotifyToServer发送数据时失败或数据丢失

**错误代码**：
```python
# ❌ 错误示例
data = self.CreateEventData()
data["position"] = (100, 64, 100)  # tuple不支持序列化!
self.NotifyToClient(playerId, "TeleportEvent", data)  # 失败!
```

**原因**：EventData不支持tuple类型，只支持dict、list、str、int、float、bool

**解决方案**：使用list代替tuple

```python
# ✅ 正确示例
data = self.CreateEventData()
data["position"] = [100, 64, 100]  # 使用list
data["items"] = [{"id": 1}, {"id": 2}]  # 嵌套dict和list可以
self.NotifyToClient(playerId, "TeleportEvent", data)  # 成功
```

**支持的数据类型**：
- ✅ dict
- ✅ list
- ✅ str
- ✅ int, float, bool
- ❌ tuple（不支持）
- ❌ 对象实例（不支持）

**详见**：[开发规范.md - EventData序列化限制](./开发规范.md#23-eventdata序列化限制-)

---

### 问题4：模块导入错误 - TypeError: 'module' object is not callable

**症状**：启动时报错`TypeError: 'module' object is not callable`

**错误代码**：
```python
# ❌ 错误示例 - modMain.py
import modConfig  # ❌ 错误导入方式

@Mod.Binding(name=modConfig.MOD_NAME, version=modConfig.MOD_VERSION)
class MyMod(object):
    pass
```

**原因**：网易引擎会扫描模块的所有属性，尝试实例化具有`MOD_NAME`的对象。使用`import modConfig`会将模块对象暴露，导致引擎尝试调用`modConfig()`

**解决方案**：使用`from ... import ...`

```python
# ✅ 正确示例 - modMain.py
from modConfig import MOD_NAME, MOD_VERSION, SERVER_SYSTEMS, CLIENT_SYSTEMS

@Mod.Binding(name=MOD_NAME, version=MOD_VERSION)
class MyMod(object):
    pass
```

**子目录导入**：
```python
# ✅ 正确 - 在子目录文件中使用完整路径
# 文件位置: MyMod/systems/MySystem.py
from MyMod.modConfig import MOD_NAME

# ❌ 错误 - 在子目录中使用相对导入
from modConfig import MOD_NAME  # ImportError!
```

**详见**：[开发规范.md - 模块导入规范](./开发规范.md#7-模块导入规范-critical)

---

### 问题5：子目录导入失败 - ImportError: No module named modConfig

**症状**：在子目录文件中导入modConfig报ImportError

**错误代码**：
```python
# ❌ 错误示例
# 文件位置: MyMod/systems/MySystem.py
from modConfig import MOD_NAME  # ImportError: No module named modConfig
```

**原因**：`modConfig.py`位于包根目录，子目录文件不在同一层级

**解决方案**：使用完整包路径

```python
# ✅ 正确示例
# 文件位置: MyMod/systems/MySystem.py
from MyMod.modConfig import MOD_NAME
from MyMod.config.game_config import GAME_RULES
```

**导入规则对照表**：

| 文件位置 | 正确导入方式 | 错误导入方式 |
|---------|-------------|-------------|
| `MyMod/modMain.py` | `from modConfig import MOD_NAME` | ✅ 同级目录可用 |
| `MyMod/systems/MySystem.py` | `from MyMod.modConfig import MOD_NAME` | ❌ `from modConfig import ...` |
| `MyMod/config/game_config.py` | `from MyMod.modConfig import MOD_NAME` | ❌ `from modConfig import ...` |

---

### 问题6：实体无法交互

**症状**：创建的实体在游戏中无响应或无法交互

**可能原因**：

1. **实体在错误的维度创建**
```python
# ✅ 确保维度ID正确
dimension_id = 0  # 主世界
entityId = self.CreateEngineEntityByTypeStr(
    'minecraft:zombie',
    (100, 64, 100),
    (0, 0),
    dimension_id  # 确认维度ID
)
```

2. **实体类型标识符错误**
```python
# ✅ 使用正确的实体标识符
"minecraft:zombie"  # 原版实体
"namespace:custom_entity"  # 自定义实体
```

3. **区块未加载**
```python
# 检查实体是否在加载的区块中
if self.gameComp.IsEntityAlive(entityId):
    print("实体存活")
else:
    print("实体不存在或区块未加载")
```

---

### 问题7：客户端事件无法接收

**症状**：服务端发送的事件客户端收不到

**排查步骤**：

1. **检查事件名称是否一致**
```python
# 服务端
self.NotifyToClient(playerId, "UpdateUI", data)  # 事件名: UpdateUI

# 客户端
self.ListenForEvent("MyMod", "MyServerSystem", "UpdateUI", self, self.OnUpdate)  # 事件名必须一致
```

2. **验证客户端System是否注册**
```python
# modConfig.py
CLIENT_SYSTEMS = [
    ("MyClientSystem", "MyMod.systems.MyClientSystem.MyClientSystem"),
]
```

3. **确认使用了正确的发送方法**
```python
# 发送给单个玩家
self.NotifyToClient(playerId, "UpdateUI", data)

# 广播给所有玩家
self.BroadcastToAllClient("GlobalMessage", data)

# 发送给多个玩家
self.NotifyToMultiClients([playerId1, playerId2], "TeamMessage", data)
```

---

### 问题8：配置文件读取失败

**症状**：配置文件无法加载或数据不正确

**排查步骤**：

1. **检查文件路径**
```python
# 使用完整包路径
from MyMod.config.game_config import GAME_RULES
```

2. **检查配置文件语法**
```python
# ✅ 正确的配置格式
GAME_RULES = {
    "MAX_PLAYERS": 16,
    "RESPAWN_TIME": 5
}

# ❌ 错误：末尾逗号、注释等
GAME_RULES = {
    "MAX_PLAYERS": 16,  # 注释会导致错误
}
```

3. **确认Python 2.7兼容性**
```python
# ✅ 使用Unicode字符串
name = u"玩家"

# ❌ 避免使用Python 3特性
name = f"玩家{id}"  # f-strings不支持
```

---

### 问题9：AOI感应区不生效

**症状**：CreateEntityAOI创建的感应区不触发回调

**可能原因**：

1. **感应区范围超过2000格**
```python
# ❌ 错误：范围太大
dimComp.CreateEntityAOI(
    dimension=0,
    name="huge_zone",
    aabb=(0, 0, 0, 3000, 256, 3000),  # 长宽3000格，超过限制!
    func=self.OnEnter
)

# ✅ 正确：使用多个小感应区
dimComp.CreateEntityAOI(
    dimension=0,
    name="zone1",
    aabb=(0, 0, 0, 1000, 256, 1000),  # 长宽1000格
    func=self.OnEnter
)
```

2. **区块未加载**
- AOI感应区所在的区块必须被加载才能工作
- 建议在玩家活动频繁的区域设置感应区

3. **回调函数未正确绑定**
```python
# ✅ 确保回调函数正确定义
def OnEnter(self, args):
    if args['isEnter']:
        print("实体进入:", args['entityId'])
```

---

### 问题10：联机大厅Mod不生效

**症状**：在单人模式正常，联机大厅不工作

**可能原因**：

1. **modAPI版本号不匹配**
- 检查manifest.json中的modAPI版本
- 确保与联机大厅要求的版本一致

2. **客户端代码导入了服务端模块**
```python
# ❌ 错误：客户端代码导入服务端模块
# MyClientSystem.py
from MyMod.systems.MyServerSystem import MyServerSystem  # 导致客户端失效!

# ✅ 正确：完全隔离双端代码
# 客户端只导入客户端模块
# 服务端只导入服务端模块
```

3. **配置文件不一致**
- 确保world_behavior_packs.json与manifest.json一致

[⬆️ 返回目录](#目录)

---

### 问题11：Claude Code斜杠命令报错"空文本块"

**症状**：运行斜杠命令（如 `/mc`、`/initmc`、`/mc-docs`、`/mc-docs --gen`）时出现API错误

**错误信息**：
```
API Error: 400 {"type":"error","error":{"type":"invalid_request_error",
"message":"messages: text content blocks must be non-empty"}}
```

或

```
API Error: 400 {"type":"error","error":{"type":"invalid_request_error",
"message":"messages.0.content.X.text: cache_control cannot be set for empty text blocks"}}
```

**原因**：命令文件中存在空文本块或使用了空的动态占位符

**问题示例**：

```markdown
# ❌ 错误示例1：使用动态占位符
**用户任务**：{{ARGUMENTS}}
# 当用户不提供参数时，{{ARGUMENTS}}为空，产生空文本块

# ❌ 错误示例2：纯流程说明，缺少实际指令
## 执行流程
### 步骤1：扫描项目结构
...
# 这只是说明文档，没有给Claude的直接指令
```

**解决方案**：

1. **更新工作流项目**到最新版本：
```bash
cd "d:\EcWork\基于Claude的MODSDK开发工作流"
git pull
```

2. **重新部署到MODSDK项目**：
```bash
# 在你的MODSDK项目目录中
modsdk-deploy

# 或使用npx
npx github:EaseCation/基于Claude的MODSDK开发工作流
```

**正确的命令文件格式**：

```markdown
# ✅ 正确示例：/mc 命令
## 任务执行指令

你现在需要按照 CLAUDE.md 中定义的标准工作流程执行以下任务。

**⚠️ 重要提示**：
- 如果用户提供了任务描述，请执行该任务
- 如果用户未提供任务描述，请提示用户
```

```markdown
# ✅ 正确示例：/mc-docs 命令
请验证项目文档的质量和完整性，生成验证报告。

## 你的任务

1. **扫描项目结构**
   - 使用Grep搜索所有System类
   - 统计Systems总数

2. **检查文档覆盖率**
   ...
```

**预防措施**：

自定义斜杠命令时，确保：
1. 文件开头有明确的指令（不能只有标题）
2. 不使用可能为空的动态占位符
3. 指令部分使用"你的任务"或"请执行"等明确的动词

[⬆️ 返回目录](#目录)

---

### 问题12：游戏卡顿，服务器TPS降低

**症状**：游戏运行时出现明显卡顿，服务器TPS（每秒Tick数）从20降至10以下

**可能原因**：

1. **Update()中执行复杂计算**
```python
# ❌ 错误：每帧执行复杂计算
class LeaderboardServerSystem(ServerSystem):
    def Update(self):
        # 每帧（20次/秒）重新计算排行榜
        players = self.GetAllPlayers()  # 100个玩家
        for player in players:
            score = self.CalculateScore(player)  # 复杂计算：5ms/玩家
        self.SortLeaderboard()  # 排序：10ms
        # 总耗时：100×5ms + 10ms = 510ms → 服务器卡死！
```

**解决方案**：使用节流
```python
# ✅ 正确：降低执行频率
class LeaderboardServerSystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.tick_counter = 0

    def Update(self):
        self.tick_counter += 1
        # 每100 tick（5秒）执行一次
        if self.tick_counter % 100 == 0:
            self.UpdateLeaderboard()

    def UpdateLeaderboard(self):
        players = self.GetAllPlayers()
        for player in players:
            score = self.CalculateScore(player)
        self.SortLeaderboard()
```

**性能对比**：
- 优化前：510ms × 20次/秒 = 10,200ms/秒（完全卡死）
- 优化后：510ms ÷ 5秒 = 102ms/秒（流畅）

**详见**：[性能优化完整指南.md - Update频率控制](./性能优化完整指南.md#三update频率控制)

---

### 问题13：网络延迟高，事件不同步

**症状**：玩家操作延迟明显，客户端和服务器数据不一致

**可能原因**：

1. **高频RPC调用**
```python
# ❌ 错误：每帧发送位置更新
class PlayerClientSystem(ClientSystem):
    def Update(self):
        # 每帧（60fps）发送位置
        self.NotifyToServer('Player_PositionUpdate', {
            'pos': self.GetPlayerPos()
        })
        # 带宽占用：60次/秒 × 40字节 = 2.4KB/秒/玩家
        # 100个玩家 = 240KB/秒 → 网络拥塞！
```

**解决方案**：节流 + 仅在变化时发送
```python
# ✅ 正确：降低频率 + 增量更新
class PlayerClientSystem(ClientSystem):
    def __init__(self, namespace, systemName):
        ClientSystem.__init__(self, namespace, systemName)
        self.last_sent_pos = None
        self.tick_counter = 0

    def Update(self):
        self.tick_counter += 1
        current_pos = self.GetPlayerPos()

        # 每20帧（约0.33秒）检查一次
        if self.tick_counter % 20 == 0:
            # 只有位置变化超过0.5格才发送
            if self.last_sent_pos is None or self.DistanceTo(current_pos, self.last_sent_pos) > 0.5:
                self.NotifyToServer('Player_PositionUpdate', {'pos': current_pos})
                self.last_sent_pos = current_pos
```

**性能提升**：带宽占用降低95%

2. **事件数据过大**
```python
# ❌ 错误：传输完整玩家列表
self.NotifyToClient(playerId, 'Leaderboard_UpdateEvent', {
    'players': [
        {'id': 'player1', 'name': 'Alice', 'score': 1000, 'level': 50, ...},
        # ... 100个玩家完整数据
    ]
})  # 数据大小：~50KB → 延迟50-100ms

# ✅ 正确：只传输必要数据 + 分页
self.NotifyToClient(playerId, 'Leaderboard_UpdateEvent', {
    'top10': [
        {'id': 'player1', 'score': 1000},  # 只传ID和分数
        # ... 最多10条
    ],
    'page': 1
})  # 数据大小：~500B → 延迟5ms
```

**详见**：[网络架构与通信.md - 网络优化技巧](./网络架构与通信.md#五网络优化技巧)

---

### 问题14：System未使用RegisterView导致性能下降

**症状**：世界中实体数量增加后，游戏明显变卡

**错误代码**：
```python
# ❌ 错误：每帧遍历所有实体（包括方块、物品、粒子等）
class HealthRegenServerSystem(ServerSystem):
    def Update(self):
        # 遍历世界中所有实体（假设10,000个实体）
        for entity in self.GetAllEntities():
            if self.IsPlayer(entity):  # 只有100个玩家
                self.RegenHealth(entity)
        # 浪费99%的CPU时间检查非玩家实体！
```

**原因**：默认情况下，System的`Update()`会遍历**所有Entity**，包括方块、物品、粒子等

**解决方案**：使用RegisterView过滤实体
```python
# ✅ 正确：只监视拥有Health组件的实体
class HealthRegenServerSystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        # 注册视图：只监视拥有Health组件的实体
        self.RegisterView('PlayersWithHealth', ['Minecraft:Health'])
        self.Create()

    def Update(self):
        # 只遍历玩家和生物（约100个）
        for entity in self.GetEntitiesInView('PlayersWithHealth'):
            self.RegenHealth(entity)
```

**性能对比**：

| 场景 | 无RegisterView | 有RegisterView | 性能提升 |
|------|---------------|---------------|---------|
| 100个玩家 | 遍历10,000个实体 | 只遍历100个玩家 | **100倍** |

**检测方法**：
```bash
# 搜索缺少RegisterView的System
grep -L "RegisterView" behavior_packs/*/scripts/server/*ServerSystem.py
```

**详见**：[性能优化完整指南.md - RegisterView深度优化](./性能优化完整指南.md#二registerview深度优化)

---

### 问题15：内存持续增长最终崩溃

**症状**：游戏运行一段时间后内存占用持续增长，最终崩溃

**可能原因**：

1. **忘记取消事件监听**
```python
# ❌ 错误：没有Destroy方法
class TempSystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.ListenForEvent('MyMod', 'OtherSystem', 'Event', self, self.OnEvent)

    def OnEvent(self, args):
        pass

    # 缺少Destroy方法 → System销毁后监听器仍存在 → 内存泄漏
```

**解决方案**：在Destroy中取消监听
```python
# ✅ 正确：取消所有监听
class TempSystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.ListenForEvent('MyMod', 'OtherSystem', 'Event', self, self.OnEvent)

    def OnEvent(self, args):
        pass

    def Destroy(self):
        # 取消所有监听
        self.UnListenAllEvents()
```

2. **缓存未清理**
```python
# ❌ 错误：玩家离开后缓存未清理
class DataServerSystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.player_cache = {}  # 玩家数据缓存

    def GetPlayerData(self, playerId):
        if playerId not in self.player_cache:
            self.player_cache[playerId] = self.LoadData(playerId)
        return self.player_cache[playerId]

    # 玩家离开时未清理 → 内存泄漏

# ✅ 正确：玩家离开时清理缓存
class DataServerSystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.player_cache = {}
        self.ListenForEvent(
            serverApi.GetEngineNamespace(),
            serverApi.GetEngineSystemName(),
            'OnPlayerLeft',
            self,
            self.OnPlayerLeave
        )

    def OnPlayerLeave(self, args):
        playerId = args['playerId']
        # 清理玩家缓存
        if playerId in self.player_cache:
            del self.player_cache[playerId]
            print("[INFO] 清理玩家缓存:", playerId)
```

**内存监控**：

> ⚠️ **白名单限制**: `gc` 模块不在Python白名单内，无法使用！
>
> **替代方案**:
> 1. 优化代码逻辑，避免内存泄漏（见下方最佳实践）
> 2. 使用引用计数管理（手动释放大对象）
> 3. 在 `Destroy()` 中清理所有缓存

```python
# ❌ 错误 - gc模块不在白名单
# import gc

# ✅ 正确 - 手动管理内存
class MemoryMonitor(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.player_cache = {}  # 玩家缓存
        self.tick_counter = 0

    def Update(self):
        self.tick_counter += 1
        # 每5秒清理离线玩家缓存
        if self.tick_counter % 100 == 0:
            self._CleanupOfflinePlayers()

    def _CleanupOfflinePlayers(self):
        """手动清理离线玩家缓存，避免内存泄漏"""
        online_players = set(serverApi.GetPlayerList())
        cached_players = set(self.player_cache.keys())

        # 清理离线玩家
        for player_id in (cached_players - online_players):
            del self.player_cache[player_id]
            print("[Memory] 清理离线玩家缓存:", player_id)

        print("[Memory] 当前缓存玩家数:", len(self.player_cache))

    def Destroy(self):
        # 清理所有缓存
        self.player_cache.clear()
        self.player_cache = None
```

**详见**：[反模式识别清单.md - 反模式11：忘记取消事件监听](./反模式识别清单.md#反模式11-忘记取消事件监听-🟠-high)

[⬆️ 返回目录](#目录)

---

## 调试技巧

### 1. 使用print()输出日志

**推荐的日志格式**：
```python
print("[INFO] [系统名] 消息内容")
print("[ERROR] [系统名] 错误信息")
print("[DEBUG] [系统名] 调试信息")

# 示例
print("[INFO] [MySystem] 玩家加入:", playerId)
print("[ERROR] [MySystem] 数据不存在:", dataId)
print("[DEBUG] [DataManager] 玩家数据:", playerData)
```

**日志级别控制**：
```python
DEBUG = False  # 生产环境设为False

if DEBUG:
    print("[DEBUG] 调试信息")
```

### 2. 日志输出位置

- **游戏日志**：网易MC客户端的日志目录
- **控制台输出**：游戏运行时的控制台窗口
- **Studio日志**：开发者工具的脚本测试日志窗口

### 3. 异常捕获

```python
try:
    # 你的代码
    result = self.doSomething()
except Exception as e:
    print("[ERROR] 发生异常:", e)
    import traceback
    traceback.print_exc()  # 打印完整堆栈
```

### 4. 变量调试

```python
# 打印变量类型和值
print("[DEBUG] type(obj):", type(obj), "value:", obj)

# 打印字典内容
import json
print("[DEBUG] data:", json.dumps(data, indent=2, ensure_ascii=False))

# 打印列表长度
print("[DEBUG] 列表长度:", len(myList), "内容:", myList)
```

### 5. 事件调试

```python
def OnPlayerJoin(self, args):
    """调试事件参数"""
    print("[DEBUG] OnPlayerJoin args:", args)
    # 查看所有参数
    for key, value in args.items():
        print("[DEBUG] {}: {}".format(key, value))
```

### 6. 热更新调试

**支持热更新的修改**：
- ✅ 函数内实现修改
- ✅ 方法逻辑修改
- ✅ 事件回调修改

**需要完整重载的修改**：
- ❌ 新增类
- ❌ 新增文件
- ❌ 全局变量修改
- ❌ 类结构修改

**完整重载方法**：
1. 保存退出当前存档
2. 重新进入存档

[⬆️ 返回目录](#目录)

---

## 网易引擎限制

### 1. Python版本限制
- ✅ **必须兼容Python 2.7**
- ❌ 不能使用Python 3专有特性（f-strings、async/await等）

### 2. 实体标识符限制
- ✅ 实体类型名称建议不超过32字符
- ✅ 使用小写字母和下划线：`mygame_entity_custom`
- ✅ 使用命名空间：`namespace:entity_type`

### 3. 维度管理限制
- ✅ 维度ID必须是整数
- ✅ 推荐使用10000以上避免冲突
- ✅ 主世界：0，地狱：1，末地：2

### 4. 事件系统限制
- ✅ 事件数据必须可序列化（dict、list、str、int、float、bool）
- ❌ 不能传递tuple（会导致序列化失败）
- ❌ 不能传递对象实例
- ✅ 自定义事件名称建议使用驼峰命名：`PlayerCoinChangeEvent`

### 5. 组件限制
- ⚠️ 某些组件在特定维度或游戏模式下可能不可用
- ⚠️ 客户端组件不能在服务端使用，反之亦然
- ⚠️ 需要在Create()中获取组件，不能在__init__中

### 6. AOI限制
- ⚠️ AOI感应区长宽不能超过2000格
- ⚠️ 推荐使用多个小感应区
- ⚠️ 区块卸载时IsEntityAlive返回False

### 7. 双端隔离限制
- ❌ 服务端不能GetSystem获取客户端系统
- ❌ 客户端不能GetSystem获取服务端系统
- ✅ 必须使用NotifyToClient/NotifyToServer通信

[⬆️ 返回目录](#目录)

---

## 性能问题

### 问题1：Tick性能低

**症状**：Update()方法执行频繁，导致性能下降

**解决**：使用计数器降低频率

```python
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.tickCount = 0
        self.Create()

    def Update(self):
        self.tickCount += 1

        # 每20 tick（1秒）执行一次
        if self.tickCount % 20 == 0:
            self.oncePerSecond()

        # 每100 tick（5秒）执行一次
        if self.tickCount % 100 == 0:
            self.oncePerFiveSeconds()

    def oncePerSecond(self):
        """每秒执行一次的逻辑"""
        pass

    def oncePerFiveSeconds(self):
        """每5秒执行一次的逻辑"""
        pass
```

### 问题2：内存泄漏

**症状**：长时间运行后内存占用持续增长

**解决**：

1. **在Destroy()中清理资源**
```python
def Destroy(self):
    # 取消事件监听
    self.UnListenAllEvents()

    # 清理数据
    self.players.clear()
    self.entities.clear()
```

2. **及时删除不需要的数据**
```python
def OnPlayerLeave(self, args):
    playerId = args['playerId']
    # 清理玩家数据
    if playerId in self.players:
        del self.players[playerId]
```

### 问题3：大量日志影响性能

**症状**：频繁打印日志导致性能下降

**解决**：使用DEBUG开关

```python
DEBUG = False  # 生产环境设为False

def logDebug(self, msg):
    if DEBUG:
        print("[DEBUG] [MySystem]", msg)

def logInfo(self, msg):
    print("[INFO] [MySystem]", msg)

# 使用
self.logInfo("系统初始化完成")  # 总是输出
self.logDebug("调试信息")  # 仅开发环境输出
```

### 问题4：频繁创建EventData

**症状**：频繁调用CreateEventData()和NotifyToClient()

**解决**：批量发送或降低频率

```python
# ❌ 错误：每个玩家单独发送
for playerId in playerIds:
    data = self.CreateEventData()
    data["message"] = "Hello"
    self.NotifyToClient(playerId, "UpdateUI", data)

# ✅ 正确：使用批量发送
data = self.CreateEventData()
data["message"] = "Hello"
self.NotifyToMultiClients(playerIds, "UpdateUI", data)  # 性能更好
```

### 问题5：复杂计算在Update中执行

**症状**：复杂计算每tick都执行，导致卡顿

**解决**：

1. **降低执行频率**
```python
def Update(self):
    self.tickCount += 1
    # 每5秒执行一次复杂计算
    if self.tickCount % 100 == 0:
        self.complexCalculation()
```

2. **使用缓存**
```python
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.cache = {}  # 缓存计算结果
        self.Create()

    def getExpensiveData(self, key):
        # 先检查缓存
        if key in self.cache:
            return self.cache[key]

        # 执行复杂计算
        result = self.complexCalculation(key)
        self.cache[key] = result
        return result
```

3. **异步处理**
```python
# 将复杂计算分散到多个tick
class MySystem(ServerSystem):
    def __init__(self, namespace, systemName):
        ServerSystem.__init__(self, namespace, systemName)
        self.processQueue = []  # 待处理队列
        self.Create()

    def Update(self):
        # 每tick只处理一个任务
        if self.processQueue:
            task = self.processQueue.pop(0)
            self.processTask(task)

    def addTask(self, task):
        """添加任务到队列"""
        self.processQueue.append(task)
```

[⬆️ 返回目录](#目录)

---

## 获取帮助

如果以上方法都无法解决问题：

1. 📖 **查看相关文档**：
   - [开发规范](./开发规范.md) - CRITICAL规范
   - [开发指南](./开发指南.md) - 完整开发流程
   - [CLAUDE.md](../CLAUDE.md) - AI工作流参考

2. 🔍 **查阅官方文档**：
   - [网易MODSDK Wiki](https://github.com/EaseCation/netease-modsdk-wiki) - 官方API文档
   - [基岩版Wiki](https://github.com/Bedrock-OSS/bedrock-wiki) - 原版机制参考

3. 📝 **记录问题详情**并寻求帮助：
   - 详细描述问题现象
   - 提供错误日志
   - 附上相关代码片段
   - 说明已尝试的解决方法

[⬆️ 返回目录](#目录)

---

## 📚 相关文档

### 重要参考
- [开发规范](./开发规范.md) - 避免常见错误
- [开发指南](./开发指南.md) - 正确开发方式
- [快速开始](./快速开始.md) - 环境配置问题

### 架构文档
- [CLAUDE.md](../CLAUDE.md) - 核心架构参考
- [项目状态](./项目状态.md) - 功能清单

---

**[⬆️ 返回顶部](#问题排查指南)** | **[🏠 返回首页](../README.md)**

---

_最后更新: 2025-11-11 | 文档版本: 4.1 (新增网络/性能/架构问题)_
