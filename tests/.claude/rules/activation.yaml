# Activation Stage Rules - 任务激活阶段规则
# Version: v3.0 Final
# 此阶段由 UserPromptSubmit Hook 自动完成，AI无需介入

display_name: "任务激活"
description: "任务初始化与类型识别（自动执行）"

# 允许的工具列表（此阶段AI不介入，由Hook自动完成）
allowed_tools: []

# 前置条件（起始阶段，无前置条件）
preconditions: []

# 路径规则
path_rules: {}

# 语义规则
semantic_rules: {}

# 完成条件
completion_condition:
  type: "automatic"
  auto_advance: true
  next_step: "planning"
  description: "UserPromptSubmit Hook识别任务类型后自动推进到planning"

# AI引导提示
ai_guidance: |
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  📌 任务激活阶段
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  此阶段由系统自动完成任务类型识别：
  - BUG修复任务
  - 功能设计任务

  你无需执行任何操作，系统将自动推进到Planning阶段。
  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# 任务类型识别规则
task_type_detection:
  bug_fix:
    keywords:
      - "BUG"
      - "修复"
      - "错误"
      - "不对"
      - "失败"
      - "异常"
      - "问题"
      - "bug"
      - "fix"
      - "error"
    description: "用户描述包含BUG相关关键词"

  feature_design:
    keywords:
      - "实现"
      - "添加"
      - "功能"
      - "新增"
      - "调整"
      - "feature"
      - "add"
      - "implement"
      - "create"
    description: "用户描述包含功能开发相关关键词"

  default: "feature_design"  # 默认识别为功能设计
