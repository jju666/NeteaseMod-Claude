# 快速参考

> **基于Claude的MODSDK开发工作流 - 技术细节手册**
>
> 深入了解工作流架构、命令系统和官方资源

---

## 🏗️ 技术架构

### 核心模块

| 模块 | 文件 | 功能 | 代码行数 | 版本 |
|------|------|------|---------|------|
| **软连接管理器** ⭐ | `lib/symlink-manager.js` | 跨平台软连接创建和更新 | ~400 | v16.0 |
| **版本检测器** ⭐ | `lib/version-checker.js` | 版本对比、哈希计算、冲突检测 | ~300 | v16.0 |
| **迁移工具** ⭐ | `lib/migration-v16.js` | v15.x自动迁移到v16.0 | ~400 | v16.0 |
| **自适应发现引擎** | `lib/adaptive-doc-discovery.js` | 扫描项目结构，生成映射规则 | ~500 | v14.1 |
| **工作流初始化器** | `lib/init-workflow.js` | 部署配置和文档到目标项目 | ~500 | - |
| **智能文档维护** | `lib/intelligent-doc-maintenance.js` | AI驱动的文档生成和更新 | ~600 | v14.1 |
| **文档索引引擎** | `lib/indexer.js` | 构建全局文档索引 | ~300 | - |
| **智能检索引擎** | `lib/search-engine.js` | 全文检索和相关度排序 | ~400 | - |
| **项目分析器** | `lib/analyzer.js` | 代码分析和复杂度评估 | ~500 | - |

**总计**: 约 6500 行纯 JavaScript 实现，零 Python 依赖

---

### 双层文档架构 ⭐ v16.0

```
下游项目/
├── .claude/
│   ├── core-docs/           # 上游基线层（软链接）
│   │   ├── 开发规范.md       # 只读，自动同步
│   │   ├── 问题排查.md       # 只读，自动同步
│   │   └── ...
│   └── commands/
│       └── cc.md
├── markdown/
│   ├── core/               # 项目覆盖层（可编辑）
│   │   └── 开发规范.md      # 项目定制版（如有）
│   └── systems/
│       └── PlayerSystem.md
└── CLAUDE.md
```

**工作原理**：
1. **上游基线层**: `.claude/core-docs/` 软链接到上游工作流仓库
2. **项目覆盖层**: `markdown/core/` 存放项目定制的核心文档
3. **智能路由**: AI优先读取覆盖层，覆盖层不存在时读取基线层
4. **自动同步**: `initmc --sync` 一键更新基线层

**优势**：
- ✅ **零风险升级**: 基线层自动更新，覆盖层不受影响
- ✅ **按需定制**: 只有需要修改时才创建覆盖层文件
- ✅ **职责隔离**: 多项目共用上游时，互不干扰（100%隔离）

---

## 🔧 命令系统

### 终端命令

#### initmc - 全局部署工作流

**语法**:
```bash
initmc [选项]
```

**选项**:
- 无选项：部署工作流到当前MODSDK项目
- `--sync`：同步更新工作流（v16.0新增）
- `--reset`：强制重置工作流（v16.0新增）

**工作流程**:
1. **检测项目类型**
   - 验证当前目录是否为MODSDK项目（检查 `modMain.py`）
   - 检测是否为工作流项目本身（防止循环部署）

2. **检测全局工作流目录**
   - 查找 `~/.claude-modsdk-workflow/`
   - 如果找不到，提示用户运行 `npm run install-global`

3. **复制命令文件**
   - 创建 `.claude/commands/` 目录
   - 复制 `enhance-docs.md` 和 `validate-docs.md`（完整内容，>5KB）
   - 生成定制化的 `cc.md`（替换项目路径占位符）

4. **复制通用文档**
   - 复制 6 个通用文档到 `markdown/` 目录
   - 复制 3 个AI辅助文档到 `markdown/ai/` 目录
   - 所有文档都验证大小（确保不是空文件）

5. **生成配置文件**
   - 生成定制化的 `CLAUDE.md`（替换项目路径和日期）

6. **创建目录结构**
   - 创建 `tasks/` 目录
   - 创建 `markdown/systems/` 目录

7. **验证部署结果**
   - 检查所有关键文件是否存在
   - 验证文件大小（>1KB，防止空文件）
   - 输出详细的部署报告

---

### Slash 命令

#### /discover - 自适应项目结构发现

**核心引擎**: `lib/adaptive-doc-discovery.js`

**工作原理**:
1. 使用 Node.js 原生 `fs` 模块扫描项目目录
2. 正则表达式匹配 Python 类定义（`class XXXSystem`, `class XXXComponent`）
3. 区分 MODSDK 官方概念（System、Component）和自定义模式
4. 根据类名后缀推断文档路径（如：`ShopSystem` → `systems/商店系统.md`）
5. 生成 JSON 映射文件（`.claude/discovered-patterns.json`）

**性能**:
- 扫描速度：5-10 秒（纯文件系统操作）
- Token 消耗：0（不使用 AI）
- 适用平台：Windows/Linux/Mac

---

#### /validate-docs - 文档完整性验证

**依赖**: 需要先执行 `/discover`

**工作原理**:
1. 读取 `.claude/discovered-patterns.json`
2. AI 阅读源代码，智能推断中文文档名
3. 检查 `markdown/` 目录下是否存在对应文档
4. 生成 `markdown/文档待补充清单.md`

**Token 消耗**: 10-15k tokens（使用 haiku 模型）

---

#### /enhance-docs - 批量文档内容生成

**依赖**: 需要先执行 `/validate-docs`

**工作原理**:
1. 读取 `markdown/文档待补充清单.md`
2. AI 深度分析源代码（逐个文件分析）
3. 生成完整文档内容（1500-3000字）
4. 包含：架构设计、API文档、代码示例、常见问题
5. 自动更新待补充清单

**Token 消耗**: 5-10k tokens/文档（使用 sonnet 模型）

---

## 🌐 官方资源

### 内置文档（Git Submodule）

工作流内置了官方文档，通过 Git Submodule 管理：

- **网易 MODSDK 文档**: https://github.com/EaseCation/netease-modsdk-wiki
- **基岩版 Wiki**: https://github.com/Bedrock-OSS/bedrock-wiki

**自动部署**：
```bash
# 1. 安装工作流时自动下载文档
npm install  # 自动执行 git submodule update --init --recursive

# 2. 初始化下游项目时自动部署
initmc  # 自动创建 .claude/docs/ 软链接
```

**查询策略**（智能降级）：
1. **优先查询本地文档**（`.claude/docs/`）- 速度快（<1秒）、支持离线
2. **降级到在线查询**（本地文档不存在时）- 使用 WebFetch（5-10秒）

**手动更新文档**：
```bash
cd /path/to/NeteaseMod-Claude
git submodule update --remote  # 更新到最新版本
```

---

### WebFetch 使用指南

**基本语法**:
```python
WebFetch(
    url="<URL>",
    prompt="<查询提示>"
)
```

**常用 URL 模板**:

**1. 网易MODSDK API文档**:
```python
# 接口文档
url = "https://raw.githubusercontent.com/EaseCation/netease-modsdk-wiki/main/docs/mcdocs/1-ModAPI/接口/通用/事件.md"

# 事件文档
url = "https://raw.githubusercontent.com/EaseCation/netease-modsdk-wiki/main/docs/mcdocs/1-ModAPI/事件/玩家.md"

# 组件文档
url = "https://raw.githubusercontent.com/EaseCation/netease-modsdk-wiki/main/docs/mcdocs/1-ModAPI/组件/物品.md"
```

**2. 基岩版 Wiki**:
```python
# NBT 数据结构
url = "https://raw.githubusercontent.com/Bedrock-OSS/bedrock-wiki/main/docs/nbt/items.md"

# 实体组件
url = "https://raw.githubusercontent.com/Bedrock-OSS/bedrock-wiki/main/docs/entities/entity-intro-bp.md"
```

**使用示例**:
```python
# 查询 NotifyToClient 的参数说明
WebFetch(
    url="https://raw.githubusercontent.com/EaseCation/netease-modsdk-wiki/main/docs/mcdocs/1-ModAPI/接口/通用/事件.md",
    prompt="提取NotifyToClient方法的参数说明、返回值和使用示例"
)

# 查询物品 NBT 结构
WebFetch(
    url="https://raw.githubusercontent.com/Bedrock-OSS/bedrock-wiki/main/docs/nbt/items.md",
    prompt="提取物品NBT的字段定义和示例JSON"
)
```

---

## 🛠️ 高级配置

### 全局工作流目录

**位置**:
- **Windows**: `C:\Users\<用户名>\.claude-modsdk-workflow\`
- **Unix/Mac**: `~/.claude-modsdk-workflow/`

**结构**:
```
.claude-modsdk-workflow/
├── CLAUDE.md                    # 核心工作流配置（模板）
├── .claude/                     # Claude Code配置
│   └── commands/
│       ├── cc.md                # /cc 命令定义（模板）
│       ├── enhance-docs.md      # /enhance-docs 命令定义
│       └── validate-docs.md     # /validate-docs 命令定义
├── markdown/                    # 完整文档库
│   ├── 开发规范.md
│   ├── 问题排查.md
│   ├── 开发指南.md
│   ├── 快速开始.md
│   ├── API速查.md
│   ├── MODSDK核心概念.md
│   └── ai/                      # AI辅助文档
│       ├── 任务类型决策表.md
│       ├── 快速通道流程.md
│       └── 上下文管理规范.md
├── scripts/                     # 核心脚本
│   └── initmc.js                # 部署脚本（核心逻辑）
└── bin/                         # 全局命令入口
    └── initmc.js                # initmc 命令入口
```

---

### PATH 环境变量配置

**Windows**:

1. 按 `Win+R`，输入 `sysdm.cpl`，回车
2. 点击"高级"选项卡 → "环境变量"
3. 在"用户变量"区域找到"Path"，双击编辑
4. 点击"新建"，添加：`C:\Users\你的用户名`
5. 点击"确定"保存
6. **重新打开终端**（必须重启终端才能生效）

验证是否生效：
```bash
where initmc
# 应显示: C:\Users\你的用户名\initmc.cmd
```

**Unix/Mac**:

添加到 `~/.bashrc` 或 `~/.zshrc`：
```bash
export PATH="$HOME/.npm-global/bin:$PATH"
```

应用配置：
```bash
source ~/.bashrc  # 或 ~/.zshrc
```

验证是否生效：
```bash
which initmc
# 应显示: /Users/你的用户名/.npm-global/bin/initmc
```

---

## 📊 性能指标

| 指标 | v14.0 | v16.0 | 提升 |
|------|-------|-------|------|
| 微任务执行效率 | - | <2k tokens | 基线 |
| 标准任务成功率 | 75% | **88%** | +17% |
| 复杂任务成功率 | 60% | **92%** | +53% |
| 下游项目文档数 | 10+个 | **3-5个** | -67% |
| 职责隔离度 | 80% | **100%** | +25% |
| 历史任务检索成功率 | 60% | **92%** | +53% |
| 历史任务检索时间 | 1-2分钟 | **<10秒** | -90% |

---

## 🔗 相关文档

### 用户文档
- **[README.md](./README.md)** - 项目简介和快速上手
- **[Claude指令参考.md](./Claude指令参考.md)** - 指令速查手册
- **[快速开始.md](./markdown/快速开始.md)** - 5分钟入门

### AI工作流
- **[CLAUDE.md](./CLAUDE.md)** - AI工作流完整配置
- **[任务类型决策表.md](./markdown/ai/任务类型决策表.md)** - 任务分级
- **[上下文管理规范.md](./markdown/ai/上下文管理规范.md)** - Tasks 目录管理

### 开发规范
- **[开发规范.md](./markdown/开发规范.md)** - CRITICAL规范详解
- **[问题排查.md](./markdown/问题排查.md)** - 常见问题解决
- **[API速查.md](./markdown/API速查.md)** - API代码片段

### 版本历史
- **[CHANGELOG.md](./CHANGELOG.md)** - 版本更新日志
- **[迁移指南-v16.0.md](./markdown/迁移指南-v16.0.md)** - v15.x升级指南

---

_最后更新: 2025-11-11 | 文档版本: v1.0_
