# MODSDK工作流真·自适应机制实施进度

## 📋 会话上下文

**当前任务**: 实施方案A - 真正的自适应文档发现机制
**开始时间**: 2025-11-10
**当前进度**: 约50%完成
**预计剩余时间**: 2-3小时

---

## 🎯 核心目标

将MODSDK工作流从"硬编码项目特定结构"升级为"100%通用自适应框架"

### 问题背景

**原问题**:
- 工作流硬编码了presets/、states/、config/等项目特定目录
- 但MODSDK官方只定义System和Component两个核心概念
- 导致无法适配使用其他组织方式的项目

**调研发现**（基于官方文档审定）:
- 网易MODSDK Wiki: 只定义ServerSystem/ClientSystem和Component
- 官方制作规范: 只要求entities/和textures/目录
- presets/、states/、managers/等都是项目自定义，不应硬编码

**解决方案**:
- 方案B（已完成）: 最小改造，移除硬编码，说明自适应机制
- 方案A（进行中）: 实现真正的自适应发现，AI运行时动态识别组件类型

---

## ✅ 已完成的工作

### 1. 方案B - 最小改造（已完成并提交）

**改造内容**:
- CLAUDE.md: 移除presets/states/config/硬编码，改为自适应描述
- validate-docs.md: 通用化组件搜索模式
- enhance-docs.md: 从路径推断改为类名推断
- 项目状态.md: 移除外部项目引用

**Git Commit**: `6638713`
```
refactor: 工作流真·通用化重构 - 基于官方MODSDK文档审定
- 移除35处硬编码位置
- 对齐MODSDK官方架构（System+Component）
- 版本: v13.0 → v14.0
```

### 2. 方案A - 自适应机制（进行中）

**已完成**:

#### a) 核心引擎 - adaptive-doc-discovery.js（350行）
路径: `lib/adaptive-doc-discovery.js`

**功能**:
- 自动扫描behavior_packs目录
- 识别MODSDK官方概念（System、Component）
- 推断项目自定义模式（通过类名后缀统计）
- 推断文档目录结构
- 生成组件→文档路径映射规则
- 输出JSON配置到 `.claude/discovered-patterns.json`

**核心逻辑**:
```javascript
// 1. 扫描所有Python类
const allClasses = await this.scanAllClasses();

// 2. 识别MODSDK官方概念
// - System: 类名以ServerSystem/ClientSystem结尾
// - Component: 调用RegisterComponent（TODO）

// 3. 推断自定义模式
// - 提取类名后缀（State、Preset、Manager等）
// - 出现3次及以上认为是项目使用的模式

// 4. 推断文档目录
// - State → states/
// - Preset → presets/
// - 检查markdown/下是否已存在对应目录

// 5. 生成路径映射
// ShopServerSystem → markdown/systems/商店系统.md
// WaitingState → markdown/states/等待状态.md
```

#### b) Slash命令 - /discover
路径: `.claude/commands/discover.md`

**用途**: 让用户手动运行自适应发现工具
**命令**: `node lib/adaptive-doc-discovery.js`
**输出**: 发现报告 + `.claude/discovered-patterns.json`

#### c) 测试示例项目
路径: `example-project/`

**结构**:
```
example-project/
└── behavior_packs/
    └── behavior_pack_test/
        └── scripts/
            ├── ShopServerSystem.py      # System
            ├── TeamServerSystem.py      # System
            ├── states/
            │   ├── WaitingState.py      # State模式
            │   ├── PlayingState.py      # State模式
            │   └── EndingState.py       # State模式
            └── presets/
                ├── ShopPreset.py        # Preset模式
                └── TeamPreset.py        # Preset模式
```

**预期发现结果**:
- 2个System类
- 3个State模式类（应识别为自定义模式）
- 3个Preset模式类（应识别为自定义模式）

---

## ⚠️ 当前问题

### 问题1: Windows下execSync调用grep返回空结果

**现象**:
```bash
# 手动测试有效
$ grep -r "^class " "path/to/behavior_packs" --include="*.py"
[正常输出8个类]

# Node.js execSync调用返回空
const result = execSync(`grep -r "^class " ...`);
// result = ""
```

**原因**: 可能的编码问题或路径转义问题

**解决方案**:
1. 使用Node.js原生API替代grep（推荐）
2. 或使用globby + 文件读取
3. 或修复execSync调用

**代码位置**: `lib/adaptive-doc-discovery.js:112-138`

### 问题2: .claude目录不存在

**现象**: 保存JSON时报错 ENOENT

**解决**: 在exportToJSON前检查并创建目录
```javascript
const outputDir = path.dirname(outputPath);
if (!fs.existsSync(outputDir)) {
  fs.mkdirSync(outputDir, { recursive: true });
}
```

---

## 📋 待完成任务清单

### 阶段1: 修复核心引擎（优先级P0）

- [ ] **修复scanAllClasses方法**
  - 方案1: 使用Node.js原生API替代grep
  - 方案2: 使用globby + fs.readFile
  - 位置: `lib/adaptive-doc-discovery.js:108-139`

- [ ] **修复exportToJSON方法**
  - 创建.claude目录（如果不存在）
  - 位置: `lib/adaptive-doc-discovery.js:328-333`

- [ ] **测试验证**
  - 在example-project上运行
  - 验证discovered-patterns.json输出正确

### 阶段2: 集成到文档维护工具（优先级P1）

- [ ] **集成到/validate-docs**
  - 读取discovered-patterns.json
  - 动态生成组件搜索模式
  - 动态生成文档路径推断规则
  - 位置: `.claude/commands/validate-docs.md`

- [ ] **集成到/enhance-docs**
  - 读取discovered-patterns.json
  - 动态生成用户选项（基于实际发现的组件类型）
  - 从类名推断文档路径（使用映射规则）
  - 位置: `.claude/commands/enhance-docs.md`

### 阶段3: 文档和测试（优先级P2）

- [ ] **更新CLAUDE.md**
  - 补充/discover命令说明
  - 补充自适应机制工作流程

- [ ] **创建使用示例**
  - 在README中添加使用说明
  - 录制演示视频（可选）

- [ ] **全流程测试**
  - /discover → /validate-docs → /enhance-docs
  - 在不同项目结构上验证

### 阶段4: Git提交（优先级P0）

- [ ] **创建Git commit**
  ```
  feat: 实现真·自适应文档发现机制（方案A）

  - 新增lib/adaptive-doc-discovery.js自适应引擎
  - 新增/discover命令
  - 集成到/validate-docs和/enhance-docs
  - 测试示例项目验证
  - 版本: v14.0 → v14.1
  ```

---

## 🔑 核心设计原则

### 1. 只保留MODSDK官方定义的核心概念

**官方概念**:
- System: ServerSystem/ClientSystem
- Component: 调用RegisterComponent注册

**项目自定义** (不应硬编码):
- State、Preset、Manager、Controller、Handler等

### 2. 零配置，完全基于代码分析

**不依赖**:
- 配置文件
- 用户手动指定
- 预设的目录结构

**只依赖**:
- 实际代码扫描
- 统计分析（类名后缀频率）
- 目录结构检测

### 3. 向后兼容

**已存在的文档**:
- 检测markdown/下已存在的目录
- 优先使用已存在的目录结构
- 不强制修改

### 4. 可扩展性

**未来可能的组件类型**:
- Service、Util、Helper、Decorator等
- 都能自动识别，无需修改代码

---

## 💡 技术实现细节

### 组件类型识别算法

```javascript
// 1. 提取类名后缀
const match = className.match(/([A-Z][a-z]+)$/);
// WaitingState → State
// ShopPreset → Preset
// PayManager → Manager

// 2. 统计频率
const suffixCounts = {};
for (const cls of allClasses) {
  const suffix = extractSuffix(cls.className);
  suffixCounts[suffix] = (suffixCounts[suffix] || 0) + 1;
}

// 3. 阈值筛选（出现3次及以上）
for (const [suffix, count] of Object.entries(suffixCounts)) {
  if (count >= 3) {
    // 认为是项目使用的模式
    patterns[suffix] = {
      count,
      docDir: guessDocDir(suffix) // State → states/
    };
  }
}
```

### 文档路径映射规则

```javascript
// 输入: 类名
// 输出: 文档路径模板

function inferDocPath(className, patterns) {
  // 1. 检查是否是System
  if (className.endsWith('ServerSystem') || className.endsWith('ClientSystem')) {
    return {
      dir: 'markdown/systems/',
      template: '{chineseName}系统.md'
    };
  }

  // 2. 检查自定义模式
  for (const [type, pattern] of Object.entries(patterns)) {
    if (className.endsWith(pattern.suffix)) {
      return {
        dir: `markdown/${pattern.docDir}`,
        template: `{chineseName}${pattern.suffix}.md`
      };
    }
  }

  // 3. 默认
  return {
    dir: 'markdown/components/',
    template: '{chineseName}.md'
  };
}
```

### discovered-patterns.json格式

```json
{
  "timestamp": "2025-11-10T...",
  "projectRoot": "/path/to/project",
  "behaviorPackPath": "/path/to/behavior_packs",
  "patterns": {
    "officialConcepts": {
      "systems": [
        {
          "className": "ShopServerSystem",
          "filePath": ".../ShopServerSystem.py",
          "fileDir": ".../scripts"
        }
      ],
      "components": []
    },
    "customPatterns": {
      "state": {
        "suffix": "State",
        "count": 3,
        "docDirCandidate": "states/",
        "docDirExists": false,
        "examples": [...]
      },
      "preset": {
        "suffix": "Preset",
        "count": 3,
        "docDirCandidate": "presets/",
        "docDirExists": false,
        "examples": [...]
      }
    }
  }
}
```

---

## 🚀 下一步工作建议

### 立即执行（本会话或下会话）

1. **修复adaptive-doc-discovery.js的Windows兼容性**
   - 替换grep为Node.js原生API
   - 确保在example-project上测试通过

2. **完成集成测试**
   - 运行/discover
   - 验证JSON输出正确
   - 手动测试/validate-docs读取JSON

### 后续执行

3. **集成到/validate-docs和/enhance-docs**
   - 实现动态读取discovered-patterns.json
   - 改造组件搜索逻辑

4. **完整测试流程**
   - 在example-project上运行完整流程
   - 验证自适应机制工作正常

5. **Git commit并部署**
   - 提交方案A完整实现
   - 更新版本号到v14.1

---

## 📚 参考资源

### 已查阅的官方文档

1. **网易MODSDK Wiki**
   - URL: https://github.com/EaseCation/netease-modsdk-wiki
   - 关键发现:
     - API文档按功能域分类（世界、实体、方块等）
     - 只定义System和Component核心概念
     - 无presets/states/等目录要求

2. **我的世界基岩版Wiki**
   - URL: https://github.com/Bedrock-OSS/bedrock-wiki
   - 关键发现:
     - NBT结构、实体组件文档
     - 与MODSDK互补

### 项目内关键文件

- `CLAUDE.md` - 工作流核心指令（v14.0）
- `lib/adaptive-doc-discovery.js` - 自适应引擎
- `.claude/commands/discover.md` - /discover命令文档
- `.claude/commands/validate-docs.md` - 文档验证工具
- `.claude/commands/enhance-docs.md` - 文档生成工具
- `example-project/` - 测试示例

---

## 🎯 成功标准

方案A完成后应达到：

1. **功能完整性**
   - ✅ /discover能正确识别任意MODSDK项目的组件类型
   - ✅ /validate-docs能动态适配发现的组件类型
   - ✅ /enhance-docs能动态生成选项和路径

2. **通用性**
   - ✅ 适配只使用System的项目
   - ✅ 适配使用State模式的项目
   - ✅ 适配使用Preset/Manager/任意自定义模式的项目
   - ✅ 适配混合多种模式的项目

3. **用户体验**
   - ✅ 零配置，自动发现
   - ✅ 清晰的发现报告
   - ✅ 文档自动推断路径
   - ✅ 向后兼容已有项目

4. **代码质量**
   - ✅ Windows/Linux/Mac跨平台兼容
   - ✅ 详细注释和文档
   - ✅ 完整测试覆盖

---

## 💬 给下一个会话的Claude

你好！我是上一个会话的Claude。

**你的任务**: 继续完成方案A的实施

**当前卡点**: adaptive-doc-discovery.js在Windows下grep命令返回空

**建议行动**:
1. 先用Node.js原生API重写scanAllClasses方法
2. 在example-project上测试验证
3. 然后继续集成到/validate-docs和/enhance-docs

**关键提醒**:
- 遵循"只保留MODSDK官方概念"原则
- 所有自定义模式都要动态发现，不硬编码
- 测试在不同项目结构上的适配性

加油！这个工作非常有意义，完成后工作流就能真正做到100%通用了！🚀

---

**最后更新**: 2025-11-10
**下次会话**: 继续修复Windows兼容性问题
